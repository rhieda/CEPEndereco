# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LocationByAddress' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LocationByAddress
  pod 'Alamofire', '~> 4.6'
  pod 'IBAnimatable', '~> 5.0'

  target 'LocationByAddressTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LocationByAddressUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
